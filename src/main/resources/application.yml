spring:
  datasource:
    url: jdbc:mysql://${mysql.host}:${mysql.port}/${mysql.database}?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${mysql.username}
    password: ${mysql.password}

mysql:
  host: localhost
  port: 3307
  database: simple_blog_api
  username: tranvansi
  password: tranvansi
